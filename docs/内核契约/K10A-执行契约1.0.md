# K10A Execution Contract 1.0 内核契约

## 阶段状态

K10A 已验收。A1 Command/Query Version、A2 Execution Scope、A3 分层执行与 External Effect Runtime、A4 Effect Guard 均已完成代码、持久化、自动化和多配置门禁。该结论只代表 Execution Contract 1.0 收口；K10B–K10F 尚未完成，因此不得宣布 Kernel 1.0 Frozen。

本阶段只修改 Application Kernel、Infrastructure Adapter、自动化测试和中文文档。不会实现 CAD、CAM、Machine、Process、Collision、控制器、Qt GUI、产品 CLI/RPC、AI 或其他上层模块。

## A1：版本化执行身份

### 稳定键

Command 与 Query 的注册身份分别为：

```cpp
struct CommandKey {
    CommandName name;
    Version version;
};

struct QueryKey {
    QueryName name;
    Version version;
};
```

CommandRequest 与 QueryRequest 必须显式携带 Version。Registry 按稳定键存储，允许同一名称的多个版本同时存在；目录快照按名称、Version 确定性排序。

### 解析策略

`VersionResolution::Exact`：

- 只接受与请求 Version 完全相等的注册项；
- 不选择更高 minor/patch；
- Workflow 与 Script 固定使用此策略。

`VersionResolution::Compatible`：

- 只考虑与请求相同 major 的注册项；
- 候选 Version 必须大于或等于请求 Version；
- 多个候选存在时选择最高 Version；
- 不跨 major，不回退到低于请求的版本。

该兼容规则表示调用方愿意接受同一 major 下不低于其最低要求的契约。Registry 在 AppKernel Ready 后冻结，因此一次进程生命周期内解析结果不会漂移。

### 弃用与错误

Descriptor 使用 `ContractStatus::Active` 或 `ContractStatus::Deprecated`。Deprecated 不等于删除：精确或兼容解析仍可命中，但 CommandResponse/QueryResponse 必须返回实际 `resolvedVersion` 与 `contractStatus`，使 Host 可以明确提示迁移。

F5C 明确在四类 Command handler 和 Query handler 的注册入口拒绝其他 ContractStatus 值，分别返回 `Command.InvalidStatus` / `Query.InvalidStatus`；不能先安装未知状态再由响应回传。模块忽略该注册错误也不能进入 initialize。该补充门禁的实现与证据见 [F5C 记录](K10F-F5C-执行边界与状态终审.md)。

失败行为固定为：

- 名称从未注册：`Command.NotFound` / `Query.NotFound`；
- 名称存在但没有满足策略的版本：`Command.UnsupportedVersion` / `Query.UnsupportedVersion`；
- 完全相同的名称和版本重复注册：`Command.AlreadyRegistered` / `Query.AlreadyRegistered`。

版本失败发生在参数 Schema、Capability、Revision 与 handler 之前，不产生领域写入或外部副作用。

### 幂等与编排

Command 的进程内请求签名包含请求 Version 与解析策略；持久幂等签名升级为 `lasercnc.command-signature.v2`，包含请求 Version、解析策略和实际解析 Version。同一 IdempotencyKey 在重启后若解析到不同契约，必须冲突并 fail-closed，不能重放到另一个版本；旧格式签名也不能与新请求静默等价。

Workflow/Script 定义已经携带 Command/Query 精确 Version。Registry Freeze 时按 `CommandKey`/`QueryKey` 检查；不存在的精确版本会拒绝 AppKernel 进入 Ready。可靠编排不能使用 Compatible，因为新增版本不应改变已持久 Workflow 的执行目标。

## A2：Execution Scope

CommandRequest 与 QueryRequest 不再把 ProjectId、DocumentId 作为无条件必填字段，而是共同持有：

```cpp
struct ExecutionContext {
    SessionId sessionId;
    optional<ProjectId> projectId;
    optional<DocumentId> documentId;
};
```

SessionId 始终存在，用于调用主体、Capability 和审计；Descriptor 的 ExecutionScope 决定应用状态上下文的严格形状：

| Scope | ProjectId | DocumentId | 含义 |
|---|---|---|---|
| Global | 禁止 | 禁止 | 全局内核操作，不绑定 Session 状态容器 |
| Session | 禁止 | 禁止 | 绑定当前调用会话，但不绑定 Project/Document |
| Project | 必须 | 禁止 | Project 级操作 |
| Document | 必须 | 必须 | Document 级操作 |

Global 与 Session 的字段形状相同，但契约语义不同；调用方不能通过多带 ProjectId 把 Global 请求隐式提升为 Project 请求。DocumentId 没有 ProjectId 时永远非法。

Scope 校验在参数 Schema、Capability、Revision 和 handler 之前执行，错误为 `Command.ScopeMismatch` 或 `Query.ScopeMismatch`。Query 只有在 Document Scope 下取得不可变 Document Snapshot，并再次校验 Project ownership；Global、Session、Project Query 不接触 DocumentStore。

异步 ReadOnly Command 将可选 ProjectId/DocumentId 原样投影到 TaskRequest，因此支持四类 Scope，且不会为 Global/Session 请求伪造 Document。同步 ICommandHandler 仍是 ApplicationTransaction 内的 DocumentWrite，因此注册时强制使用 Document Scope；不能借 Scope 放宽现有事务边界。

## A3：分层执行路径

### 同步 ReadOnly

同步 ReadOnly Command 使用独立的 `IReadOnlyCommandHandler` 和 `ReadOnlyCommandContext`，不会创建 `ApplicationTransaction`，也不会向 handler 暴露可变 Document。四类 Scope 均可注册；只有 Document Scope 才取得不可变 Document Snapshot，并复核 Project ownership 与可选 Project Revision 前置条件。

注册阶段固定拒绝以下不一致描述：

- 非 Synchronous 的 ReadOnly handler；
- 把 ReadOnly handler 声明为 DocumentWrite 或 External Effect；
- ReadOnly Command 声明 undoable；
- 同步 ReadOnly Command 声明使用 Command 幂等存储。

同步 ReadOnly 的版本解析、Scope、Schema 与 Capability 校验均在 handler 前完成。结果仍执行 Result Schema 校验，并返回实际解析版本与契约状态；日志或其他执行后观测失败只进入 `postExecutionErrors`，不能反转已经成功的只读结果。

### DocumentWrite

同步 DocumentWrite 继续使用 `ICommandHandler` 和 `ApplicationTransaction`，只允许 Document Scope。事务、Revision、Journal、Event 与持久幂等语义保持原路径，不与 ReadOnly handler 共用可变执行上下文。

### External Effect

FileSystemWrite、Publish、MachineControl、Motion、LaserControl 统一使用独立 `IExternalEffectHandler` 与 `EffectExecutor`，不能借用 ReadOnly 或 ApplicationTransaction。Descriptor 必须声明：

- `idempotent=true`，且请求必须提供稳定 IdempotencyKey；
- 有效 `ReplayPolicy`；
- 至少一个 EffectGuardId；
- 至少一个 ResourceClaim，且 units 大于零；
- `undoable=false`。

这里的 `idempotent=true` 表示命令必须进入稳定身份与持久执行记录，并不把 `Never` 或 `ReconcileOnly` 的外部动作宣称为天然幂等。ReadOnly、异步 ReadOnly 与 DocumentWrite 反向禁止携带 External Effect 元数据，三条路径不会静默混用。

SQLite schema v7 的 `external_effects` 保存幂等键、完整执行签名及摘要、ReplayPolicy、执行状态、可选结果及摘要、开始/更新时间。签名覆盖 Session/Project/Document 上下文、命令请求版本与实际解析版本、参数、Revision 前置条件、副作用等级、Guard 集合和资源声明；同一键出现任一签名漂移都会冲突。

执行状态与恢复结论固定如下：

| ReplayPolicy | 崩溃或 handler 失败后的状态 | RecoveryDisposition | 自动重放 | 同键显式重试 |
|---|---|---|---|---|
| Safe | Interrupted | Interrupted | 禁止 | 允许 |
| Idempotent | Interrupted | Interrupted | 禁止 | 允许 |
| ReconcileOnly | ReconcileRequired | ReconcileRequired | 禁止 | 禁止 |
| Never | Indeterminate | Indeterminate | 禁止 | 禁止 |

Kernel 初始化只把上次进程遗留的 Executing 按上表归一化，不调用 handler。Completed 请求只返回持久结果，并重新校验强摘要和当前 Result Schema。Safe/Idempotent 的显式重试必须使用同一 IdempotencyKey 与完全相同的签名；ReconcileRequired/Indeterminate 只能由后续领域的显式对账流程处理，不能直接重试原动作。

handler 返回失败、抛出异常、结果 Schema 不符或完成结果落盘失败时，EffectExecutor 都先尝试把记录转为相应恢复状态，再返回带原因链的错误。保守状态不会因为 handler 声称失败就假定外部世界未发生变化。

## A4：Effect Guard 与资源租约

`EffectGuardRegistry` 按稳定 EffectGuardId 注册 `IEffectGuard`，并在 Kernel Ready 时冻结。External Effect 引用不存在的 Guard、重复 Guard 或未配置持久化时，AppKernel 启动失败。

执行顺序固定为：

```text
Version / Scope / Schema
→ Capability
→ Document ownership / Revision Preconditions
→ EffectGuard（声明顺序）
→ ResourceManager lease
→ durable claim
→ IExternalEffectHandler
→ Result Schema
→ durable Completed
```

Guard 拒绝、Capability 拒绝、Revision 冲突或资源不可用均不会创建 durable claim，也不会调用 handler。资源租约复用 Task/Scheduler 的 `ResourceManager`，所有返回路径均释放；相同独占资源的并发 Effect 只能有一个进入 durable claim。

Kernel 不提供碰撞算法、Machine interlock、MotionSafetyPermit 或 Laser safety 判断。后续 Machine/Safety Domain 只能实现 Guard 并经注册接入，不能绕过 EffectExecutor。

## 后续阶段

按收口规划进入 K10B Runtime Project/Document Lifecycle。K10A 不实现运行期 Document 创建/关闭、History、ModuleRegistrar、ExecutionGateway、ObjectType/Asset 或最终可靠性认证。

## 当前自动化证据

- 同名多版本注册、排序与重复精确键拒绝；
- Exact、Compatible、Deprecated、NotFound、UnsupportedVersion；
- Runtime 在 handler 前拒绝不支持版本，并在响应返回实际版本与弃用状态；
- Workflow/Script 精确版本冻结检查；
- 同步 ReadOnly 四类 Scope、Document Snapshot/ownership/Revision 前置条件与 handler 前 Scope 拒绝；
- ReadOnly 注册形态约束，以及只读结果 Schema 和执行后观测隔离；
- External Effect 注册形态、稳定签名、专用持久 claim 与 Completed 重放；
- Safe/Idempotent 显式重试，以及 ReconcileOnly/Never 的 fail-closed 恢复状态；
- Capability → Preconditions → Guard → Resource → durable claim → handler 次序；
- Guard/持久化缺失时启动拒绝、独占资源并发拒绝、状态/策略篡改拒绝；
- 既有持久幂等、独立进程恢复与 Headless 契约保持通过；
- A1 基线 VS2022 x64 Debug、Release：各 131/131 CTest；
- A2 当前 VS2022 x64 Debug：134/134 CTest；
- A3 ReadOnly 子节点 VS2022 x64 Debug：136/136 CTest；
- K10A 完整 VS2022 x64 Debug/Release：各 145/145 CTest；
- Debug 145 项连续 20 轮，共 2,900 次测试执行；
- Production-only Release：31 个 VS 工程，测试/contract/Catch2 目标为 0，Catch2 source 不存在；
- 架构扫描：60 个 Kernel 公共头文件、115 个生产源文件通过。

外部 Effect 的真正独立进程异常退出门禁属于 K10F Crash Recovery 认证；K10A 已通过真实 SQLite 关闭/重开恢复测试，证明启动只归一状态且不会自动调用 handler，但不提前宣称完成 K10F。K10A 也不证明任何 Machine、Motion、Laser、文件发布实现或物理安全能力。
