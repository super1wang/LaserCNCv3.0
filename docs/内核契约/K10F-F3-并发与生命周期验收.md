# K10F F3：并发与生命周期验收

## 状态

F3 已验收。F3A/F3B 覆盖规划中的九类并发/生命周期项目，并通过下述当前代码汇总门禁。本次作为可靠性阶段重要节点提交远端。F4/F5 未完成，Kernel 1.0 不处于 Frozen 状态。

## 规划逐项映射

| 原规划项目 | 当前证据 |
| --- | --- |
| 并发 Query | F3A：16 路 Query 同时处于 handler，返回相同只读材料，不增加 Revision/Journal/History |
| 并发相同 Idempotency Command | F3A：16 路同键请求，只有 1 次 handler/提交和 15 次 replay；新内核仍重放原回执 |
| Revision conflict | F3A：8 个基于同一 Revision 的候选，1 个持久胜者、7 个冲突；恢复后无失败候选材料 |
| Task cancellation race | F3B：真实 BS worker，完成前/后及 8 路 cancel 与完成竞争；检查终态、资源复用和重启 |
| Workflow cancellation race | F3B：真实两步 DocumentWrite，完成前/后及 8 路 cancel 与执行完成竞争；检查已接受提交、步骤 attempt 和恢复终态 |
| Document close vs Query | F3A：Query 活动时拒绝 close；Closing 期间 16 路 Query 全部拒绝，close/open 后恢复执行 |
| Document close vs Task | F3B：Running/CancelRequested Task 阻塞 close；真实完成后 close/open 成功 |
| shutdown vs active runtime | F3A：活动 Command/Query/Transaction 拒绝 shutdown；F3B：实际 Workflow 步骤拒绝 shutdown，Task 超时保持 Stopping/所有权，完成后可收尾 |
| Module startup failure rollback | F3B：20 轮 × 注册返回错误/注册异常/后继 start 失败；验证全部实际注册表清理、服务对象释放及健康新组合 |

每种 Command/Query/Task/Workflow 用例每次固定 20 轮，每轮新建内核/数据库。Module 用例每次执行 60 次失败组合与 60 次健康对照。竞态中的合法胜者不固定；明确序的前置/后置用例分别保证取消优先和完成优先语义，不能依赖随机竞争恰好覆盖两个次序。

## 实测修复

F3B 发现并修复 `Scheduler::requestCancel` 把已 `CancelRequested` 的运行任务再次取消为终态的问题。现在重复取消只维持请求状态，实际完成回调才回收运行容量和独占资源。失败与修复后的证据见 [F3B 记录](K10F-F3B-取消竞态与生命周期压力.md)，架构规则 117 固定此不变量。

测试辅助代码之外没有新生产能力或公开 API。F3A 首轮活动租约含义修正、F3B 模块健康对照补 executor 均属于测试构造修正，未通过放宽内核检查来使测试通过。

## 验证门禁

当前代码 Debug 全集 250/250（443.37 秒）、Release 全集 250/250（432.64 秒）通过。纯生产 Release 与架构扫描通过，31 个工程无测试/contract/Catch2 工程或 CTest 文件；公共头文件 69 个、生产源文件 133 个。两种配置均启用自有代码警告即错误。

被测生产代码与测试源码对应本地检查点 `d7e56cb`；后续 F3 汇总只更新文档，不在验证过程中改变实现。复现命令如下，均从仓库根目录执行：

```powershell
cmake --preset vs2022-debug
cmake --build --preset vs2022-debug --parallel 1 -- /nodeReuse:false
cmake --build --preset vs2022-release --parallel 1 -- /nodeReuse:false
ctest --preset vs2022-debug --output-on-failure
ctest --preset vs2022-release --output-on-failure
ctest --preset vs2022-debug -R '^Kernel (concurrency|task stress|workflow stress|module stress)' -j 4 --repeat until-fail:3 --output-on-failure
cmake -S . -B build/production-only -G 'Visual Studio 17 2022' -A x64 -DLCNC_BUILD_TESTING=OFF -DLCNC_WARNINGS_AS_ERRORS=ON
cmake --build build/production-only --config Release --parallel 1 -- /nodeReuse:false
cmake -DLCNC_SOURCE_ROOT=J:/Code/LaserCNCv3.0 -P cmake/VerifyKernelBoundaries.cmake
```

完整 F3 三次连续重复全部通过（768.56 秒）：12 个用例各执行 3 次，共 36 次测试执行；已逐项核对每个用例的实际成功记录均为 3 条。11 个运行时用例累计 660 个独立持久化轮次，模块用例累计 180 次失败组合及 180 次健康对照。没有把断言数当作测试数，也没有把开发期失败计为最终版本通过证据。

当前记录范围内达到 0 flaky、0 deadlock、0 partial state。最终日志为 `build/k10f-f3-repeat3.log`，双配置全集日志为 `build/k10f-f3b-debug-tests.log` / `build/k10f-f3b-release-tests.log`，最终纯生产构建日志为 `build/k10f-f3-production-final.log`。入口记录分别为 [F3A](K10F-F3A-命令查询与生命周期并发压力.md) 与 [F3B](K10F-F3B-取消竞态与生命周期压力.md)。本机二进制证据保留于忽略的构建目录；Git 保存源码、复现步骤与证据摘要。

## 结论范围

F3 的“0 flaky / 0 deadlock / 0 partial state”只针对记录中的测试配置、轮次及交错，不是对所有可能线程调度的数学证明。同步点有 5 秒上限和异常释放保证；CTest 进程超时只是挂起兜底，不等于所有生产 API 都有强制中断能力。

不把本矩阵视为性能/内存基线，不把显式取消解释为撤销已提交状态，不宣称任意多个 Host 可同时配置或销毁同一个 AppKernel。没有引入 GUI、CLI/RPC、AI、CAD/CAM、Machine 或 Process 模块；物理机安全与存储断电保证也不在本验收范围内。
