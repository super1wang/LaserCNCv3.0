# K10F-F4 性能与内存基线

## 状态

F4 已验收：测试工具、1k/10k/100k 正式性能与内存基线、原始证据归档及双配置回归均完成。范围仅为内核及测试代码，不增加产品 CLI、领域模块或第三方库；未修改生产 API 或实现。F5 与 Kernel Frozen 仍未完成。

## 可复现入口

`tests/benchmark/kernel_benchmark.cpp` 为 Windows 测试目标，依赖现有内核、SQLite/JSON/Hash/Snapshot 适配器及 Windows SDK Psapi。`LCNC_BUILD_TESTING=OFF` 时不生成它。运行脚本使用 PowerShell 7，按每个数据集七组、每组独立进程、全部串行执行：

```powershell
cmake --preset vs2022-debug
cmake --build --preset vs2022-release --target lasercnc_kernel_benchmark --parallel 1 -- /nodeReuse:false
pwsh -NoProfile -ExecutionPolicy Bypass -File tests/benchmark/RunKernelBaseline.ps1
```

`-ExecutionPolicy Bypass` 仅作用于本次脚本进程，不修改系统执行策略。可指定 `-ObjectCounts 1000` 预检。默认 1k、10k、100k 对象；操作预热一次、采样五次；生命周期十轮。运行目录位于忽略的 `build/kernel-baselines/run-*`，每轮独立保留日志、原始 JSON 和 SQLite/Snapshot 文件，无自动删除。索引记录源提交及脏工作区状态、源码/脚本/程序/报告 SHA-256、CPU、系统和采样参数。脚本逐组验证退出码、成功标记、报告范围、配置和样本数，不允许把 Debug 数据当作正式 Release 基线。

## 数据与计时边界

- ObjectId 为固定九位数字后缀，载荷为单个 `payload` 字段的 128 字节字符串；类型版本 1.0.0，Durable，真实字段验证器，无引用、无 Asset。实际 Value 序列化字节数写入报告。该数据集不代表 OCCT 形体、大资产或任意深度 Value。
- 每个操作保留原始样本、最小值/中位数/最大值、校验耗时和结果存活时内存。准备、完整结果校验、返回对象析构均在计时外；API 内部工作和成功/错误结果解包仍包含在操作中。`verification_ms` 还可能包含测试收尾（如 begin 后 rollback、恢复后的 shutdown），不是纯断言耗时。Transaction begin 的 ID 组装、History 的请求构造和成功回执检查包含在各自操作计时中；Global 请求预先构造。计时采用单调时钟；不清空 OS/文件/分配器缓存。五次样本仅建立本机基线，不作尾延迟或跨机器性能保证。
- `component` 使用公开组件组合，初始批量创建 N 对象并提交一次，初始 Revision=1。分别测 Document snapshot、Transaction begin（之后回滚）和单对象 commit；commit 的 begin/replace 在计时外。SQLite 模式的初始化会写 N 对象 Journal，不能与网关组初始化成本混为一谈。
- `gateway` 使用 AppKernel 和 ExecutionGateway，attach N 对象，初始 Revision=0；SQLite 组另保存基线 Snapshot。直接空 handler、Global Query/ReadOnly Command 是框架开销对照；Document Query/ReadOnly Command 包含当前文档快照复制。写命令包含完整事务、候选校验、提交与集成；不能称作纯框架开销。六次写入保留六条 History，Undo/Redo 固定反复应用最后一条；恢复起始游标的反向命令在计时外。每次写请求（含 Undo/Redo）使用唯一请求 ID。
- `journal` 使用实际 SQLite 及 N 对象 Snapshot，六条单对象变更尾部。append 的来源命令在另一内存内核准备，计时只含 Journal 写入；recover_material 返回持久化恢复材料，bootstrap_recovery 包含内核恢复/安装。恢复的配置/打开连接在 bootstrap 计时外；校验对象、Revision、History 范围，不把只读 Snapshot 当作完整启动恢复。
- `lifecycle` 独立十轮配置、bootstrap、attach，SQLite 组还保存初始 Snapshot；每轮记录启动/种子、shutdown/析构耗时和销毁后内存。全部十轮记录，不按操作预热规则排除首轮。操作组的 seed 耗时从 bootstrap/attach 开始，不含之前的模块注册与适配器配置；生命周期启动计时则包含这些配置。
- SQLite 实际 `journal_mode/synchronous/page_size/cache_size/foreign_keys` 写入报告，不人为关闭刷盘。NullLog 排除日志文件 I/O，但网关的日志组织与 Trace 集成仍属于测量路径。

## 内存解释

记录工作集、私有提交内存和进程峰值工作集，单位字节。`memory_before` 与 `memory_result_live` 不是采样期间的独立峰值；进程峰值包含初始化、校验以及该进程之前的所有操作。操作结果校验会额外复制/枚举完整对象，因此也可能抬高后续工作集和进程峰值，不能将此峰值归因于单次被测调用。

生命周期销毁后的内存用于观察跨轮次趋势，既包含内核释放后的分配器缓存，也包含小规模报告积累，不能单凭未立即回落断言泄漏或无泄漏。F5 地址检查器和可靠性门禁另行验证。

## 初始工具验证

Debug/Release 各七组 CTest 冒烟已通过（6.50 / 6.09 秒），1k 七组预检成功。首次 SQLite History 冒烟暴露的是基准程序重复请求 ID，触发 `Persistence.JournalTransactionConflict`；已修正测试请求编号，未修改生产契约或绕过持久化一致性检查。工具以 `b708339` 提交本地检查点，再在该干净版本采集以下正式基线。

## 正式测量结果（2026-09-04）

源代码检查点 `b708339`，采样时工作区干净。机器为 Intel Core i5-12600KF（10 核 / 16 逻辑处理器）、约 31.84 GiB 可见物理内存，Windows 11 10.0.27749，MSVC 19.44.35216.0，x64 Release，自有代码 `/W4 /WX`。串行采样于 06:40:06–06:41:43（UTC+08）完成，21/21 组成功，81 项操作统计、405 个正式耗时样本、60 个生命周期轮次；每个操作另有一次预热，不计入 405。

原始报告与追溯索引已归档于 [evidence/k10f-f4/index.json](evidence/k10f-f4/index.json)，21 个报告的 SHA-256 与现场索引逐项一致。全部载荷序列化为 142 字节，资产数 0；全部 SQLite 组实际 `journal_mode=delete`、`synchronous=2`、`page_size=4096`、`cache_size=-2000`、`foreign_keys=1`。本轮无 F3 压力或构建并行负载，但并未控制机器上的其他进程或关闭 OS 缓存。

### 操作中位数

单位 **ms**；单元格为“内存 / SQLite”。两模式为独立进程，不应把其差值直接当作精确 I/O 成本。

| 操作 | 1k | 10k | 100k |
| --- | ---: | ---: | ---: |
| Document snapshot | 0.341 / 0.319 | 3.402 / 3.478 | 38.688 / 35.474 |
| Transaction begin | 0.707 / 0.681 | 7.266 / 7.263 | 71.749 / 70.113 |
| Transaction commit | 1.463 / 116.154 | 14.937 / 155.172 | 175.576 / 252.085 |
| Query Global 网关 | 0.022 / 0.020 | 0.021 / 0.027 | 0.019 / 0.018 |
| Command Global 只读网关 | 0.020 / 0.020 | 0.019 / 0.021 | 0.018 / 0.015 |
| Query Document 网关 | 0.486 / 0.512 | 6.188 / 4.608 | 57.632 / 53.944 |
| Command Document 只读网关 | 0.461 / 0.499 | 6.152 / 4.753 | 51.327 / 50.496 |
| Command Document 写网关 | 2.174 / 77.291 | 27.150 / 122.607 | 276.376 / 403.598 |
| History Undo 网关 | 2.082 / 144.493 | 28.593 / 169.555 | 278.675 / 371.527 |
| History Redo 网关 | 2.390 / 133.097 | 24.601 / 172.514 | 277.810 / 410.039 |

直接空 handler 中位数约 0.0002–0.0004 ms，仅作编译后的小载荷对照，接近计时粒度，不能据此推导业务吞吐率。Global 网关中位数约 0.015–0.027 ms；Document 组随全量对象复制增长，不能使用 Global 数字描述大文档请求。

| 持久化操作 | 1k | 10k | 100k |
| --- | ---: | ---: | ---: |
| Journal append | 130.874 | 118.559 | 99.914 |
| 恢复材料 | 4.627 | 45.738 | 479.996 |
| 内核 bootstrap 恢复 | 6.175 | 61.701 | 651.150 |

恢复均为 N 对象 Snapshot + 6 条单对象变更尾部，持久化目录总材料约 0.378 / 2.755 / 26.530 MiB（含数据库和 Snapshot，不含报告）。恢复校验全部对象的 ID、类型、版本、Value、空资产列表、全部 Revision 分量、重放条数、History 提交数量及启动后游标。该结果不覆盖长 Journal、高密度 History 或大资产恢复。

SQLite 延迟有明显波动：100k Journal append 原始范围 78.650–185.310 ms，Document 写网关 368.542–452.165 ms。1k 或 10k 的某项 SQLite 中位数高于更大数据集并不证明反向规模效应；五次样本和刷盘扰动不足以支持该结论。

### 内存及释放后趋势

100k 内存组件组的稳定 Document 基线私有内存约 78.74 MiB；Snapshot 返回对象存活时约 153.94–154.98 MiB，Transaction begin 返回事务存活时约 230.23–231.07 MiB。begin 同时保留 baseDocument 和 staging，commit 还枚举完整候选，实测确认复制/校验是主要规模成本。报告中的私有提交内存与工作集不是同一指标，不直接相减。

| 100k 独立进程 | 内存模式峰值工作集 MiB | SQLite 模式峰值工作集 MiB |
| --- | ---: | ---: |
| component | 355.40 | 747.09 |
| gateway | 345.66 | 525.52 |
| lifecycle | 210.15 | 530.25 |

Journal 组峰值工作集约 525.55 MiB。组件 SQLite 的 747.09 MiB 峰值包含初始化时批量创建 100k 对象、完整 Journal 序列化与校验，不能标作单对象 commit 峰值。生命周期 SQLite 每轮的新数据库和 Snapshot 共保留约 265.19 MiB 磁盘材料，这是十轮证据积累，不是内存泄漏。

| 对象数 / 模式 | 首轮销毁后私有内存 MiB | 第十轮销毁后 MiB | 十轮销毁后最大 MiB |
| --- | ---: | ---: | ---: |
| 1000 / memory | 1.19 | 1.18 | 1.57 |
| 1000 / sqlite | 1.47 | 1.92 | 1.92 |
| 10000 / memory | 1.44 | 2.12 | 2.95 |
| 10000 / sqlite | 2.32 | 2.79 | 3.25 |
| 100000 / memory | 2.43 | 4.20 | 6.83 |
| 100000 / sqlite | 4.65 | 8.45 | 8.45 |

100k 第十轮销毁前私有内存约 107.03 / 120.80 MiB，销毁后约 4.20 / 8.45 MiB；可观察到主体文档内存释放，但 SQLite 组释放后残留由 4.65 升到 8.45 MiB，不能据此签发零泄漏。十轮不足以区分分配器缓存、框架缓存及泄漏，后续 F5 保留地址检查器门禁；本节只认证测量和趋势记录。

## 基于测量的结构判断

1. 保留现有按值快照和事务模型，不在 F4 引入 shared immutable snapshot / delta-overlay。当前固定单文档、小 Value 数据集的 100k snapshot / begin / 内存写网关分别约 39 / 72 / 276 ms；它们能在本轮完成稳定取样和状态校验，没有出现迫使本阶段改变公共模型的容量失败。但这不是“复制没有成本”或已满足未来交互预算的结论。
2. 把全量文档复制列为已知规模限制：100k Document 只读请求约 50–58 ms，显然不能承诺 60 Hz 同步交互；高频小改动也会承担 O(N) 级候选复制/遍历。当前阶段不扩展 GUI 或业务模型，也没有用户确认的交互吞吐 SLA，因此不凭假设预算启动跨事务/恢复语义的结构重构。若未来批准大文档高频读写要求，必须以本基线为对照提出内核 RFC，并重新验证隔离、Revision、Undo/Redo 与恢复，不能允许业务绕过 Gateway。
3. SQLite 单次持久写延迟及完整 Snapshot 序列化内存也是约束，不能单靠 immutable snapshot 解决。维持刷盘配置，不为改善数字弱化一致性。本轮也未承诺实时控制、外部设备、存储掉电或业务负载准入。

## 验证门禁与验收范围

正式 21 组测量与原始归档校验已完成；81 项统计从原始样本重新计算后逐项一致。当前版本 Debug 全集 257/257（299.17 秒）、Release 全集 257/257（289.58 秒）通过，均包含七组新增冒烟、既有故障注入、独立进程恢复与 F3 压力用例；两配置依次执行，不与性能采样并行。Production-only Release 通过：31 个工程，无测试/contract/Catch2/benchmark 工程，无 CTest 文件；架构扫描通过，公共头 69 个、生产源 133 个。自有代码保持警告即错误。

```powershell
ctest --preset vs2022-debug -j 4 --output-on-failure
ctest --preset vs2022-release -j 4 --output-on-failure
cmake -S . -B build/production-only -G 'Visual Studio 17 2022' -A x64 -DLCNC_BUILD_TESTING=OFF -DLCNC_WARNINGS_AS_ERRORS=ON
cmake --build build/production-only --config Release --parallel 1 -- /nodeReuse:false
cmake -DLCNC_SOURCE_ROOT=J:/Code/LaserCNCv3.0 -P cmake/VerifyKernelBoundaries.cmake
```

本机日志：`build/k10f-f4-baseline.log`、`build/k10f-f4-debug-tests.log`、`build/k10f-f4-release-tests.log`、`build/k10f-f4-production.log`；正式数据库与 Snapshot 保留于索引记录的 `build/kernel-baselines/run-*`。原始 JSON 已纳入 Git，日志及二进制不纳入版本库。

F4 验收表示规划要求的测量项目已有可复现基线及明确结构判断，不代表任意业务负载性能达标、零泄漏或硬实时准入。作为重要节点同步远端；下一节点 F5 继续隔离 ASan 配置、真实检查器验证与最终版本认证。全部必需门禁汇总前不得宣布 Kernel Frozen，也不启动上层模块。
