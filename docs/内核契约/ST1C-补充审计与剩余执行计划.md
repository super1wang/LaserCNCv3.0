# ST1C 补充审计与剩余执行计划

## 依据和范围

2026-09-04 用户确认按交叉审计结论完善计划并继续收口。基线为本地 ST1B `375e542`，远端审计基于 `2bb2e70`；外部讨论是待核验证据，不作为独立指令。原 F5C 的 31/32 表示历史测试覆盖，不表示当前只剩一个问题。

保留 ST1A/B 已验证的持久目录、轻量 ProjectRuntime 和子文档关闭协调。正确性问题先补回归，再实现、验收；小节点提交本地，完整正确性与认证大节点推送现有远端。失败日志和测试数据库保留。不扩展 CAD/CAM、Machine/Process、GUI、产品 CLI/RPC、AI。

## 剩余执行节点

| 节点 | 内容及验收 | 状态 |
| --- | --- | --- |
| C1a | ProjectRevision 恢复独立于 Open 文档集合；非零修订、全部 Detached/Removed、重启新文档提交、再次恢复；多项目与 Open/Detached 混合 | 本地检查点通过：Debug 286/286、专项 16/16、新增 2 项各 3 次、纯生产与架构通过 |
| C1b | Journal 写入端拒绝过期修订，事务内一致性检查与 C5 独占权协同；失败不发布内存/History/Event，拒绝后仍可恢复 | 本地检查点通过：Debug 308/308、专项 14/14、新增 9 项与扩展故障矩阵各 10 次、纯生产/架构/文档通过；事务内修订及首笔 Catalog/Snapshot 归属检查已实现，见 [C1b 交付](../阶段交付/2026-09-04-ST1C1b-Journal写入修订一致性.md)；下一步 C2 |
| C2a | Kernel 统一准入与 shutdown 线性化；所有 scope、Project/Document 生命周期、Task 提交、Workflow/Script start/advance 的检查后计数前窗口 | 本地检查点通过：Debug 314/314、专项 7/7、7 项各 10 次、纯生产/架构/文档通过；见 [C2a 交付](../阶段交付/2026-09-04-ST1C2a-整体准入与停止线性化.md)。下一步 C2b，不替代 C2c drain/析构和 ST1D |
| C2b | Project-only 直接租约、长期活动桥接、legacy 恢复；关闭控制操作不被自身普通执行租约阻塞，open 不要求目标已经 Open；目录版本失效语义 | 按 [C2b 子节点契约](ST1C2b-项目活动与生命周期控制.md) 完成 C2b1、C2b2a/b/c/d、C2b3 本地检查点；最新 Debug 360/360、13 项各 10 次和纯生产通过。下一步 C2c，最终签核仍归 ST1D |
| C2c | Host 生命周期线程模型、析构/drain/超时依赖保留；明确执行器销毁保证，非协作任务不能因停止超时被当成已结束 | 进行中：C2c1 本地检查点通过，Debug 368/368、专项 8/8、停止相关 10 项各 10 次和纯生产/架构通过；见 [C2c1 交付](../阶段交付/2026-09-04-ST1C2c1-停止确认与失败重试.md)。下一步 C2c2 线程/析构及持久化独占联动，见 [C2c 子节点契约](ST1C2c-停止确认与析构依赖.md) |
| C3 | DocumentId 与 Snapshot 存储键解耦；Unicode、路径/保留字符、长度边界、大小写、时钟回拨、进程重启唯一性 | 未开始 |
| C4 | 公开 attach(DocumentImage) 收口；内部恢复私有化，导入复用唯一事务/认证持久链；Host 负向编译探针，失败无可执行半状态 | 未开始 |
| C5 | 恢复前强制单活动 Host 独占，第二 Host 拒绝；SQLite 耐久配置校验与诊断；不扩展多写者数据库或宣称物理断电认证 | C5a/b 本地检查点通过：[C5b 交付](../阶段交付/2026-09-04-ST1C5b-初始化独占与活动状态保护.md)，Debug 299/299、专项 56/56、新增 5 项各 10 次及纯生产/架构通过；C1b 已承接写事务检查。完整签核仍关联 C2c、支持矩阵与 ST1D |
| C6 | 冻结 API/DTO/持久格式清单和兼容性，不承诺跨工具链 C++ ABI；长同步调用与 Task 契约、统一输入预算、有界终态保留 | 未开始，必须形成可检查契约 |
| C7 | 定向文档恢复、Schema 缓存、容量/性能回退指标分级；先测量再确定支持配置及必要优化，不盲目重构全量复制模型 | 未开始，可经证据延后优化但须明确支持上限 |
| C8 | 可重复本地门禁与证据清单、CI 分层策略；远端 Required Status/Runner/签名需实际可用配置，不把未配置写成已启用 | 未开始，外部权限问题单列，不阻塞无关本地实现 |
| ST1D | 最终三配置全集、新增故障/真实三进程恢复、连续压力、纯生产、新基线与重新签核 | 等待上述必须项闭合 |

C2b2a 已完成编排实例真实归属、完整 advance/cancel 在途保护、未保存终态阻塞和文档关闭预检，见 [C2b2a 交付](../阶段交付/2026-09-04-ST1C2b2a-编排活动与关闭预检.md)：Debug 324/324、专项 20 项各 10 次、纯生产/架构通过。下一步仍是 C2b2 的生命周期命令分类与终态/legacy 恢复，然后 C2b3，不将局部活动保护等同于 C2b2 全项完成。

C2b2b 已区分终态历史和可执行恢复，关闭/删除文档后的 Workflow 历史不再导致启动失败，且不会重开容器或重放 Handler，见 [C2b2b 交付](../阶段交付/2026-09-04-ST1C2b2b-终态工作流历史恢复.md)：Debug 328/328、14 项各 10 次与纯生产通过。下一步 C2b2 生命周期命令分类和 legacy Project-only Task/Effect 认证根，之后 C2b3；C2b2 全项仍未签核。

C2b2c 已实现受模块治理的七种固定生命周期命令，新增创建身份/墓碑保护；最终 Debug 339/339、专项 25/25、新增 11 项各 10 次和纯生产/架构通过，见 [C2b2c 交付](../阶段交付/2026-09-04-ST1C2b2c-受治理生命周期命令.md)。后续按 legacy Project-only Task/Effect 认证根 → C2b3 CatalogRevision/epoch → C2c drain/析构推进，不把控制命令的局部验收等同于 C2b2 全项完成。

C3/C4 可在 C2 后独立交付。C2b2c 新回归证明创建入口必须区分新身份与 Detached/Removed 历史；已封闭 create 路径，公开 attach、配置期装载及 Journal/Snapshot 单独来源的身份域须在 C3/C4 一并核对，不能默认已覆盖。C6/C7 可延后的实现优化必须列明理由、支持范围及不影响正确性的证据，不得通过改写验收定义关闭缺陷。C6 登记新增 LifecycleOperation/Descriptor 契约；C7 实测创建新增的认证目录读取成本。PIMPL 是可选实现，不把未签名提交等同于代码质量缺陷。

C2b2d 本地检查点通过：历史 Task/Effect 根认证、精确迁移集合及迁移后缺根拒绝已实现，最终 Debug 347/347、专项 8/8、新增 8 项各 10 次和纯生产/架构通过，见 [C2b2d 交付](../阶段交付/2026-09-04-ST1C2b2d-历史执行项目根认证.md)。上述按节点保留历史证据，当前进入 C2b3；不改变 C2c/C3/C4/C6–C8/ST1D 的完整要求。C6 限定历史保留和输入预算，C7 增加全量历史根认证及事务内二次读取的成本测量。

C2b3 本地检查点通过：版本和值在同一锁内读取的生命周期目录快照已实现，区分 Runtime epoch、项目作用域和单调计数；最终 Debug 360/360、专项 13/13、13 项各 10 次和纯生产/架构通过。见 [C2b3 交付](../阶段交付/2026-09-04-ST1C2b3-目录版本与失效.md)。下一步 C2c；C6 必须登记新增 CatalogVersion/目录 DTO、构造异常及缓存令牌非授权边界，C7 测量扫描复制和已触及作用域计数保留成本，不通过丢弃计数造成 ABA。

## C1a 修复边界与回归

Journal/Snapshot 已保存并验证 ProjectRevision；本节点修正安装阶段丢弃非 Open 文档及其携带项目修订的问题。全部已校验项目修订作为独立恢复输入，与 Open 文档镜像一起原子安装到现有 DocumentStore。装载状态不决定项目修订是否存在，不新增第二套 Transaction/History，不在生命周期目录复制未经事务同步的修订。

该修复只解决已提交修订重启回零，C1b/C5 继续处理写入端与独占权。目录变化的失效语义在 C2b/C6 明确 Project 内容修订和 CatalogRevision/epoch 的分工，不把 open/close 冒充业务提交。

回归必须通过真实 ExecutionGateway 写入非零状态，覆盖项目仍 Open、项目 Closed、文档 Removed；销毁并新建 Kernel，未打开旧文档即创建新文档，检查继承的 ProjectRevision 和局部零修订，再提交并再次启动，检查修订链、对象和 History。多项目、混合 Open/Detached、配置期新文档也需覆盖。先在旧代码运行失败并保留日志，不能只检查空文档 Revision=0。

C1a 实施与证据见 [项目修订独立恢复交付](../阶段交付/2026-09-04-ST1C1a-项目修订独立恢复.md)。C5a/b 的单 Host 所有权、耐久配置与初始化强制集成已通过本地检查点，C1b 已承接写入端事务检查，下一步进入 C2；C1a 不等于恢复了对已损坏修订链的写入权限，也不自动修补既有损坏数据库。

## C5 / C1b 实施前置核对

C1a 时 `IPersistenceBackend` 只有 SQL 与事务方法，SQLite Adapter 只显式设置外键、错误码和 busy timeout，`beginTransaction` 使用 `BEGIN IMMEDIATE`。C5a/b 已实现显式会话准入端口、实际文件独占、策略读回及初始化事务前强制接入，并完成夹具迁移与本地门禁。Benchmark 改为准入后采集实际配置，但环境记录本身不等于运行期准入门禁。

- 所有权在 schema 初始化及任何 abandoned claim 恢复之前取得，跨恢复与正常运行保留；第二个 Host 失败不能改写第一个 Host 的状态。故障代理必须转发所有权契约，不能有默认成功的伪实现。
- 验证同进程/跨进程第二实例、路径别名、正常释放和进程中断后的接管；明确连接、Host 停止、后台任务 drain 和所有权释放顺序。C2c 的未结束任务不得因 shutdown 超时提前让出所有权。
- 耐久策略必须设置并读回验证，失败拒绝启动，诊断记录实际生效值；不把默认值、配置意图或性能报告当作保证。文件系统及非本地路径的支持边界要明确，不承诺硬件断电结果。
- C1b 在同一写事务内校验项目与文档前置修订、合法后置变化和所有权；区分新提交与已验证的相同 TransactionId 重放。失败不能发布业务状态、History 或成功提交事件；失败诊断/幂等失败状态仍按既有契约处理。不能只检查全局序号或 SQL INSERT 成功。

上述是完整实施要求。C5a 组件证据不能替代 C5b 强制初始化集成，未达到全部要求前不得标记 C5 已完成；详见 [C5 设计与证据](ST1C5-持久化Host独占与耐久准入.md)。

## Project / Document 与公共入口边界

- Project 管身份、生命周期、目录和整体一致性，允许 0:N 文档；Document 管事务、业务状态、局部修订和历史。
- Module、输入 STEP、共享 View 不与 Document 一一对应；内核多项目/文档能力不强迫产品第一版同时激活它们。
- 项目业务配置未来可用普通 Manifest Document，不在 ProjectRuntime 再造业务仓库或 Undo。
- ManufacturingDocument、机台包、工程包、共享视图和 ManufacturingRelease 只记录后续组合原则，本阶段不实现；实时控制器状态不是 Document 真值。
- 入口清单区分执行活动、生命周期控制、诊断观察和内部恢复，避免关闭/打开被普通租约自阻塞。

## 签核规则

新增项映射 ST1/ST2/ST3、TX1/TX3、ET4、PE2、SA3 和 RE。历史 F5C/ST1A/B 成绩保留原提交范围，不覆盖新交错、身份域、非零修订和资源边界。清单按最终注册用例和标签生成，不硬编码旧 264/284 或故障数。

证据包含提交、工具链/依赖、配置、唯一用例数及重复次数、退出状态、日志摘要、支持边界和未执行项。自动化全绿不替代 GUI、设备安全或物理断电验收。
