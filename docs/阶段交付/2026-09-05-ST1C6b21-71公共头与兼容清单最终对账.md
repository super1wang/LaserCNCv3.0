# 2026-09-05 ST1C6b21 71 公共头与兼容清单最终对账交付

## 交付结论

C6b 的公共声明与兼容清单已经完成最终对账。当前 71 个公共头与 C6a 基线逐文件 SHA-256 全部一致，C6b1–b20 没有产生未登记的公共声明漂移；每个头都已落实到公开面、权限阶段、已验证语义和后续 C6c/d/C7 项。完整清单见 [最终对账契约](../内核契约/ST1C6b21-71公共头与兼容清单最终对账.md)。

本节点同时确定当前持久格式读写矩阵：SQLite schema 9，Journal v4（读 v1–v4）、Snapshot v3（读 v1–v3）、Command outcome v3（读 v1–v3）、Task acceptance v1、Task terminal 写 v2/读 v1–v2、Diagnostic v1、Workflow definition 规范摘要材料及 checkpoint/step wire v1、Project/Document lifecycle v1，以及独立的 `LCNCSN02`、`LCNCAS01`、`LCNCAssetRef1`、Command signature v2 和 External Effect signature v1。RecoveryReport/SessionStatus 仍为认证后的只读组合输出，不是持久写命令。

## 漏项补齐

对 `c7b3d87..9ea3429` 的生产增量提取 51 个 `Subsystem.Code` 形态的新增/变更错误码。既有 C6b 文档已覆盖 46 个，本节点补记 `Diagnostics.ExporterThrew`、`Observability.InvalidMetricValue`、`Observability.InvalidSpanName`、`Observability.InvalidSpanTimeRange`、`Persistence.InvalidTaskPayload`，最终文档追踪为 51/51。源码与回归确认它们均为真实错误分支；未发现需要追加生产修复的问题。

## 验证口径

- 生产实现基线：`9ea3429`。
- 公共头：基线 71、当前 71、摘要不一致 0。
- C6b20 紧邻实现门禁：Debug/Release 定向各 10/10，ASan 三轮 30 次，Release 全集 490/490，纯生产和 71/141 边界通过。
- 本节点为文档对账，不重新宣称代码测试发生变化；另执行 71/141 公共 API/治理/三方隔离边界、51/51 错误码文档追踪、全仓 135 份 Markdown 的 615 个本地链接和 `git diff --check`，均通过。

## 兼容与范围声明

公共头摘要一致只证明声明文件未漂移，不承诺跨编译器/工具链 ABI。C6b 收口不代表 C6、ST1 或 Kernel 1.0 Frozen；C6c 的统一预算、C6d 的同步/Task/终态保留、C7 容量、C8 门禁和 ST1D 最终三配置仍完整保留。本阶段没有扩展 CAD、CAM、Machine、Process、GUI、产品 CLI/RPC 或 AI。

下一活动节点为 C6c。由于 C6 尚未形成大节点，本交付只做本地 Git 提交，不推送远端。
